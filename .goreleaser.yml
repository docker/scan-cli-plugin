archives:
  - replacements:
      darwin: Darwin
      linux: Linux
      windows: Windows
      amd64: x86_64
checksum:
  name_template: "checksums.txt"
release:
  draft: true
  prerelease: true
changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"